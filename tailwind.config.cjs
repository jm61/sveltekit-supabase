const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
				sans: ['Josefin Sans','sans-serif']
			},
		extend: {
			
		}
	},

	plugins: []
};

module.exports = config;